# Copy to .env.local for local dev against a deployed backend, or set as
# an environment variable in your Vercel project settings.
# Defaults to http://localhost:8000/api if unset (local Django).

VITE_API_URL=https://my-backend.onrender.com/api
